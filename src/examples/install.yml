description: |
  Install kops and kubectl

usage:
  version: 2.1

  orbs:
    kube-orb: circleci/kubernetes@1.0.0

  jobs:
    build:
      docker:
        xcode: "16.04"
      steps:
        - checkout

        - kube-orb/install:
            platform: ubuntu
